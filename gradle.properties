org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# KSP 2.2.10-2.0.2 still registers generated sources via kotlin.sourceSets;
# AGP 9 blocks this by default. Remove once KSP ships a version that uses the
# android.sourceSets DSL instead.
android.disallowKotlinSourceSets=false
